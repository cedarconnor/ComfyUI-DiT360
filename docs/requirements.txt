# ComfyUI-DiT360 Dependencies
#
# NOTE: Loose version constraints to avoid conflicts with existing ComfyUI installations
# DO NOT pin exact versions unless absolutely necessary

# Core dependencies (should already be in ComfyUI)
# torch>=2.0.0,<3.0.0  # Installed with ComfyUI, don't reinstall
# torchvision>=0.15.0  # Installed with ComfyUI, don't reinstall

# Hugging Face ecosystem
transformers>=4.28.1
diffusers>=0.25.0
accelerate>=0.26.0
huggingface-hub>=0.20.0

# Model format and safety
safetensors>=0.4.2

# Optional dependencies (commented out by default)
# Uncomment if needed for advanced features

# Image processing (usually already installed)
# pillow>=10.0.0

# Advanced image operations
# opencv-python>=4.8.0

# For faster attention (optional)
# xformers>=0.0.20  # Memory efficient attention
# flash-attn>=2.0.0  # Flash attention 2 (requires CUDA)

# For model quantization (future)
# bitsandbytes>=0.41.0  # 8-bit/4-bit quantization
